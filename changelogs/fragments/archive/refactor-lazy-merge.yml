---
trivial:
  - >
    ``run`` role - Internal refactor: the effective systemd timer settings
    (``__run_logrotate_effective_timer_settings``) are now computed lazily in
    ``vars/main.yml`` instead of via an ``set_fact`` in ``tasks/init.yml``.
    No user-facing behaviour change. Added a Molecule init-only regression
    test asserting the defaults/user merge.
